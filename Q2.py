class Solution:
    def addTwoNumbers(self, list1, list2):
        
        # Will need to iterate through the link list node
        while (temp_node != None):
            print(str(temp_node.val))
            temp_node = temp_node.next
